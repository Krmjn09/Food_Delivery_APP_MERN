{
  "name": "foodmine-react",
  "version": "1.0.0",
  "description": "## 1. Demo And Installation",
  "main": "backend/index.js",
  "scripts": {
    "start": "cd backend && npm start",
    "heroku-postbuild": "cd backend && npm install && cd ../frontend && npm install && npm run build && cp -r ../frontend/build ../backend/public"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=18 <19"
  }
}
